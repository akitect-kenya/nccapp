# nccapp Installation
1. Clone the repository
2. Run both the mock apis
```
  cd remote;
  node services.js;
  node customers.js;
```
3. Start the php server
```
  cd nccapp
  php -S localhost:8000
```
